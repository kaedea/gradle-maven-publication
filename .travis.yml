language: android
sudo: true
jdk: oraclejdk8

android:
    components:
        - tools
        - platform-tools
        - build-tools-27.0.3
        - android-27
        - extra-android-m2repository
    licenses:
        - 'android-sdk-preview-license-.+'
        - 'android-sdk-license-.+'
        - 'google-gdk-license-.+'

git:
  submodules: false

# before_install:
#   - sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules
#   - git submodule init && git submodule update

script:
  - $(if [[ $TRAVIS_PULL_REQUEST == 'true' ]]; ./gradlew :publication:test --i --S; fi)
